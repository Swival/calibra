[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "calibra"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "swival>=1.0.35",
    "rich",
    "fastapi>=0.135.1",
    "uvicorn[standard]>=0.41.0",
    "jinja2>=3.1.6",
]

[project.optional-dependencies]
charts = ["matplotlib"]

[project.scripts]
calibra = "calibra.cli:main"

[dependency-groups]
dev = ["pytest", "ruff"]
website = ["markdown", "pillow"]


[tool.hatch.build.targets.sdist]
include = [
    "calibra/",
    "tests/",
    "pyproject.toml",
    "README.md",
    "LICENSE",
]
exclude = [
    "experiments/",
    "results/",
    "tasks/",
    "plans/",
    "scripts/",
    "docs/",
    "docs.md/",
    "tmp/",
]

[tool.ruff]
line-length = 100
exclude = ["tasks/"]

[tool.ruff.lint]
# Ruff 0.16 enables every rule when no selection is configured. Preserve the
# default rule set used by earlier Ruff releases.
select = ["E4", "E7", "E9", "F"]
